import { Button, TextField, Typography, ImageList, ImageListItem, FormControl,
  InputLabel, OutlinedInput, Container, Grid,AlertTitle,
  CircularProgress,Alert,Snackbar ,Slide 
 } from '@mui/material';

export const MaterialUIModule = () => {
  return {
    Alert,
    AlertTitle,
    Snackbar,
    Button,
    CircularProgress,
    Container,
    FormControl,
    Grid,
    ImageList,
    ImageListItem,
    Slide ,
    InputLabel,
    OutlinedInput,
    TextField,
    Typography,
  }
}


